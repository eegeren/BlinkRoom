"use client";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { readSharedInbox, clearSharedInbox, type SharedInbox } from "@/src/lib/share-inbox";
import { generateRoomKey, importRoomKey } from "@/src/lib/crypto/room-key";
import { encryptJson } from "@/src/lib/crypto/payload";
export default function ShareTargetPage() { const router = useRouter(), [inbox, setInbox] = useState<SharedInbox | null>(null), [busy, setBusy] = useState(false); useEffect(() => { void readSharedInbox().then(setInbox); }, []); async function accept() { if (!inbox || busy) return; setBusy(true); const roomKey = generateRoomKey(), response = await fetch("/api/rooms", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ttlHours: 24 }) }); if (!response.ok) { setBusy(false); return; } const { slug } = await response.json() as { slug: string }, key = await importRoomKey(roomKey), encryptedVerifier = JSON.stringify(await encryptJson(key, { check: "blinkroom-room-key" }, `${slug}:verifier:v1`)); const verified = await fetch(`/api/rooms/${slug}/verifier`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ encryptionVersion: 1, encryptedVerifier }) }); if (verified.ok) router.replace(`/r/${slug}#${roomKey}`); else setBusy(false); } return <main className="state-screen"><div><span className="wordmark">BlinkRoom<i /></span><h1>Ready to share.</h1><p>{inbox ? `${inbox.files.length} file${inbox.files.length === 1 ? "" : "s"}${inbox.text ? " and text" : ""} will be added to a new temporary room after you confirm.` : "No incoming shared content was found."}</p>{inbox && <div className="modal-actions"><button className="button" onClick={() => void clearSharedInbox().then(() => router.replace("/"))}>Cancel</button><button className="button filled" disabled={busy} onClick={accept}>{busy ? "Creating…" : "Create room & share"}</button></div>}</div></main>; }
