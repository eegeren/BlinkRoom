import { SeoLanding } from "@/src/components/seo-landing";
import { seoMetadata } from "@/src/lib/seo";
export const metadata = seoMetadata("/encrypted-file-sharing", "Encrypted File Sharing", "Share files from a temporary room with browser-side AES-GCM encryption, encrypted filenames, and no account required.");
export default function Page() { return <SeoLanding path="/encrypted-file-sharing" eyebrow="ENCRYPTED FILE SHARING" title="Encryption before upload. Simplicity before everything." intro="BlinkRoom encrypts files and their identifying metadata in your browser before stored transfers leave your device. The room link carries what recipients need to decrypt." proof="Browser-side AES-GCM · Key stays in the URL fragment" steps={[{ title: "Open a room", text: "Create one instantly or drop a file on the BlinkRoom homepage." }, { title: "Encrypt locally", text: "Your browser encrypts file contents and metadata before a stored upload." }, { title: "Share the link", text: "Recipients open the room link and decrypt compatible transfers in their browser." }]} sections={[{ eyebrow: "THE SECURITY MODEL", title: "What the storage layer receives.", paragraphs: ["Stored transfers reach BlinkRoom storage as encrypted binary data. File names, MIME types and original sizes are placed inside encrypted metadata rather than sent as plaintext file details.", "The room key is generated in the browser and placed after the # in the room URL. Browser URL fragments are not included in HTTP requests to BlinkRoom."], points: ["AES-GCM authenticated encryption", "Fresh IVs for encrypted chunks and metadata", "Room and item context authenticated with each encrypted chunk"] }, { eyebrow: "THE PRACTICAL PART", title: "Security without an account workflow.", paragraphs: ["There is no registration step. Create a room, add files and share its link through a channel you trust.", "A shared link grants access to the room, so treat it like a secret. BlinkRoom does not replace careful link handling or endpoint security."] }]} faqs={[{ question: "Are BlinkRoom files encrypted before upload?", answer: "Yes. Stored file contents and file metadata are encrypted in the browser with AES-GCM before upload." }, { question: "Does the server receive the room key?", answer: "The room key is stored in the URL fragment. Fragments are handled by the browser and are not sent in HTTP requests." }, { question: "Do I need an account?", answer: "No. Creating, sharing and joining a room does not require registration." }, { question: "How long is a room available?", answer: "You choose 1, 6, 24 or 72 hours. Access ends when the room expires or is destroyed earlier." }]} related={[{ href: "/private-file-sharing", label: "Private file sharing" }, { href: "/temporary-file-sharing", label: "Temporary sharing" }, { href: "/send-files-without-signup", label: "No-signup sharing" }]} />; }
